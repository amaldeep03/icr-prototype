# Sample Documents

Place test documents here for manual testing:

- `application_form.pdf` / `.jpg` / `.png`
- `government_id.pdf` / `.jpg` / `.png`
- `policy_illustration.pdf` / `.jpg` / `.png`

The backend accepts JPEG, PNG, and PDF (first page extracted via pdf2image).

Use the **Load Mock Data** button in the UI to demo the full flow without real documents.
